NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used only by RLS/tenancy tests (tests/rls-tenancy.test.ts). Must point at a
# local `supabase start` instance — never a live project. Service role key
# bypasses RLS entirely; used solely to create/delete throwaway test users.
SUPABASE_TEST_URL=
SUPABASE_TEST_ANON_KEY=
SUPABASE_TEST_SERVICE_ROLE_KEY=
